hlf_ca_version: "1.5.3-1"
hlf_orderer_version: "2.4.3-1"
hlf_orderer_nodes_num: 3
ord_service_name: "{{msp_id}}"
cert_auth: "{{msp_id}}"
ord_organization_name: "{{msp_id}}"
ca_admin_id: "{{ msp_id }}-ca-admin"
msp_admin_id: "{{ msp_id }}-msp-admin"
msp_admin_tls_id: "{{ msp_id }}-msp-admin-tls"
ord_service_enrollment_id: "{{ msp_id }}-service"
console_api_authtype: "basic"
ord_state: present